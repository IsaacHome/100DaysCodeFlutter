import 'package:flutter/material.dart';

class AirplayScreen extends StatelessWidget{

    @override
  Widget build(BuildContext context) {
    return Scaffold(
       appBar: AppBar(
         title: Text("Airplay Page"),
       ),
      body: Center(
        child: Text("AirPlay Screen"),
      ),
    );
  }
}